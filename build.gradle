apply plugin:'base'
allprojects{
	group = "com.breskeby.sample"
}

apply from:"gradle/dependencies.gradle"
apply from:'gradle/version.gradle'


subprojects{
	apply plugin:'java'
	apply plugin:'maven'

	repositories{
		mavenCentral()
	}

	dependencies{
		testCompile "junit:junit:4.11"
	}
}
