classes{
	outputs.upToDateWhen{false}
	doFirst{
		sleep 5000
		println "compilation ${project.name} done"
		throw new GradleException("classes failed")
	}
}


eclipse{
	classpath{
		file {
			whenMerged{ cpath ->
				cpath.entries = cpath.entries.findAll{ entry ->
					entry.path != "resources"			
				}
			}
		}
	}
}